<turbo-stream action="replace" target="shuffle-wish">
    <template>
        {{ form_start(shuffle_wish_form, {'attr' : {'id' : 'shuffle-wish'}}) }}
        {{ form_end(shuffle_wish_form) }}
    </template>
</turbo-stream>
